<% if obj.errors.any? %>
    <div id="error_explanation">
      <%= pluralize(obj.errors.count, "error") %> prohibited this form from being saved:
      <ul>
        <% obj.errors.full_messages.each do |message| %>
            <li><%= message %></li>
        <% end %>
      </ul>
    </div>
<% end %>