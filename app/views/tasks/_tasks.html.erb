<table class="table table-striped table-hover toggle-circle footable tableCustomStyle" data-toggle-column="first" data-empty="There are no tasks." data-show-toggle="true" >
  <thead>
    <tr>
      <th data-breakpoints="xs sm">TASK</th>
      <th data-breakpoints="xs sm">PRIORITY</th>
      <th>PROJECT/ACTIVITY</th>
      <th data-breakpoints="xs sm md">DESCRIPTION</th>
      <th>STATUS</th>
      <th data-breakpoints="xs sm md">COMPLETE</th>
      <th>ASSIGNED TO</th>
      <th data-breakpoints="xs sm md">START DATE</th>
      <th data-breakpoints="xs sm md">DUE DATE</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
  <% for task in tasks %>
      <tr>
        <td class="cursor-pointer" onclick="click_row('tasks', '<%= task.id %>')"><%= task.name %></td>
        <td class="cursor-pointer" onclick="click_row('tasks', '<%= task.id %>')"><span class="priorityDot <%= priority_class(task.priority) %>"></span><%= task.priority %></td>
        <td class="cursor-pointer" onclick="click_row('tasks', '<%= task.id %>')"><%= task.taskable_id ? task.taskable.name : "" %></td>
        <td class="cursor-pointer" onclick="click_row('tasks', '<%= task.id %>')"><span class="nowrapCol"><%= task.description %></span></td>
        <td class="cursor-pointer" onclick="click_row('tasks', '<%= task.id %>')"><%= task.status %></td>
        <td class="cursor-pointer" onclick="click_row('tasks', '<%= task.id %>')"><%= task.progress %> %</td>
        <td class="cursor-pointer" onclick="click_row('tasks', '<%= task.id %>')"><%= task.assignee.full_name %></td>
        <td class="cursor-pointer" onclick="click_row('tasks', '<%= task.id %>')"><%= task.start_date.strftime("%m/%d/%Y") if task.start_date.present? %></td>
        <td class="cursor-pointer" onclick="click_row('tasks', '<%= task.id %>')"><%= task.end_date.strftime("%m/%d/%Y") if task.end_date.present? %></td>
        <td>
          <div class="openLeft moreOptions dropdown">
            <a class="" data-toggle="dropdown" href="javascript:void(0)" aria-expanded="false" data-animation="scale-down" role="button">
              <i class="icon md-more-vert" aria-hidden="true"></i>
            </a>
            <div class="dropdown-menu" role="menu">
              <a class="dropdown-item clickable" data-href="<%= task_path(task) %>" role="menuitem"><i class="icon md-edit" aria-hidden="true"></i> Edit</a>
              <a class="dropdown-item delete-option" data-href="<%= task_path(task) %>" role="menuitem"><i class="icon md-delete" aria-hidden="true"></i> Delete</a>
            </div>
          </div>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<div class="digg_pagination">
  <%= js_will_paginate tasks, container: false %>
</div>

<script>
  $('.footable').footable();
</script>