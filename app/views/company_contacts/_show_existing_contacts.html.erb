<!-- Existing Contacts Modal -->
<div class="modal" id="showExistingContacts" aria-hidden="true" aria-labelledby="showExistingContacts" role="dialog" tabindex="-1">
  <div class="modal-dialog modal-lg" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">×</span>
        </button>
        <h6> ADD EXISTING CONTACT </h6>
      </div>
      <div class="modal-body">
        <%= render partial: 'contacts/filters', locals: { controller: 'company_contacts', action: 'show_existing_contacts' } %>
        <div class="panel">
          <div class="panel-body" id="existingContactsList">
            <%= render partial: "existing_contacts_list", locals: {contacts: @contacts} %>
            <div class="row">
              <button type="button" class="btn btn-sm white-btn" data-dismiss="modal">Cancel</button>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
<!-- End Modal -->
