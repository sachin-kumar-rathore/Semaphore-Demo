<table class="table tableCustomStyle tableImg table-hover removeTblSearch bodrTop footable footable-6 footable-filtering footable-filtering-right footable-paging footable-paging-center breakpoint-lg" data-toggle-column="first" data-empty="There are no contacts" data-show-toggle="true" id="existingContactsTable">
  <thead>
    <tr>
      <th>NAME</th>
      <th data-breakpoints="xs">ORGANIZATION</th>
      <th data-breakpoints="xs">PHONE</th>
      <th data-breakpoints="xs">EMAIL</th>
      <th>OPERATIONS</th>
    </tr>
  </thead>

  <tbody>
    <% for contact in contacts %>
      <tr>
        <td><%= contact.name %></td>
        <td><%= contact.organization ? contact.organization.name : '' %></td>
        <td><%= contact.cell_phone %></td>
        <td><%= contact.email %></td>
        <td><%= button_to "Add To Company", attach_contact_to_company_company_contact_path(@company, contact), remote: true, data: { confirm: "Are you sure you want to add this contact to company?" }, class: "btn btn-primary" %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<br/>
<div class="digg_pagination">
  <%= js_will_paginate contacts %>
</div>
<br/>

<script>
  $('.footable').footable();
</script>